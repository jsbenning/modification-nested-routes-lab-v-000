<%= form_for @song do |f| %>
<% if @artist_inst %> <!-- nested -->
  <%= artist_select(@song, @artist_inst) %><br></br> 
<% else %>
  <%= artist_select(@song) %><br></br><!-- needs dropdown -->
<% end %>

  <%= f.label :title %>
  <%= f.text_field :title %><br></br>

  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %><br></br>

  <%= f.submit %>
<% end %>
